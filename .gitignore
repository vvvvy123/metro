# 生成物：由 `python import_city.py --all --reset` 从 data/*.json 重建，
# 且含本地测试产生的设备 ID / 投票 / 评论，不入版本库。
metro.db
metro.db-journal
metro.db-wal
metro.db-shm

# 凭据：DATABASE_URL 里带密码。仓库是公开的，务必保持忽略。
.env
.env.*
!.env.example

# 数据库快照（含设备身份）应放在仓库外，这条只是兜底
*.bak.db
metro.pre-*.db

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db
